# pplayer — 円形マスク付きライブ演出プレイヤー

ライブイベントで円形スクリーンに映像・写真を投影する macOS 用 Electron アプリ。
MacBook 側に操作ウィンドウ、プロジェクター(外部ディスプレイ)側にフルスクリーン表示ウィンドウを出す。

## v2 アーキテクチャ: 素材+キューリスト(2026-07-26 拡張)

v1 の「単一スライドショー」から、「**素材ライブラリ+キューリスト**」の2層構造に拡張する。

### 素材(Material)

- **スライドショー素材**: 名前付き。写真リスト(除外・個別 fit/タイミング上書き込み)、全体タイミング(fadeIn/hold/fadeOut+ease)、fit 既定値を素材が持つ。複数作成可
- **動画素材**: mp4/mov ファイル参照。音量(素材ごと)を持つ
- **静止画素材**: 画像1枚(蓋絵などに使う)
- マスク設定(モード・反転・サイズ・オフセット)は**グローバルのまま**(物理スクリーンに紐づくため素材側に持たせない)

### キューリスト

- キュー = 素材への参照+キュー固有設定。同じ素材を複数キューから参照できる(これが v2 の眼目)
- キュー固有設定:
  - スライドショーキュー: 終了挙動(`loop`(手動で次へ) / `advance`(1周で次のキューへ自動進行) / `toStandby`(1周で蓋絵へ))
  - 動画キュー: 終了挙動(`advance` / `toStandby` / `hold`(最終フレームで静止))
  - 静止画キュー: 表示し続ける(GO で次へ)
- **蓋絵(スタンバイ)**: 特別枠。静止画素材を1つ「蓋絵」に指定でき、何も再生していないときは黒ではなく蓋絵を表示(未指定なら黒)。ワンキーで蓋絵へ退避できる
- **進行**: GO 型(Space で次のキューを発火)+任意キューのダブルクリックで即時ジャンプ。現在キューと次キューを操作画面で明示
- 優先順位: ブラックアウト > 実行中キュー > 蓋絵

### 再生エンジン

- SoT はメインプロセス(v1 踏襲)。`activeCue`(id+素材種別+開始基準時刻等)と `cues` / `materials` を AppState で管理
- スライドショー再生は v1 のタイムライン(shared/timeline.ts)をそのまま使う(素材のタイミングで buildCycles)
- 動画は表示ウィンドウの `<video>` 要素で再生し、rAF で canvas に drawImage → **マスク合成パスは写真と共通**(compositor)。再生位置・終了はレンダラーからメインに通知(終了イベントのみ。毎フレーム IPC 禁止は維持)
- 音声: 動画の音声は表示ウィンドウから出す。出力デバイスは設定でアプリ内選択(`setSinkId`)。素材ごとの音量
- 書き出し(MP4)は**スライドショー素材単位**で行う(v1 と同じパイプライン。キューリスト全体の書き出しはスコープ外)

### 永続化・移行

- electron-store / `.pplayer` は version 2(materials + cues + mask + standby + audio 設定)
- v1 データ(version 1 / 旧 store)は読み込み時に「スライドショー1」素材+それを参照する1キューへ自動変換する

## 要件(v1 からの確定仕様)

- 写真は約80枚(増える可能性あり)。jpg / png を読み込む。
- フェードは**黒を挟む方式**(クロスフェードではない): フェードイン 1.5s → 表示 5.5s → フェードアウト 1.5s = 1枚 8.5s。3つの値はそれぞれ 0.1s 刻みで調整可能。既定値は上記。
- マスクは2モード。**円形(組み込み・デフォルト)**と**カスタムマスク画像(PNG)**。
  - カスタムはアルファチャンネルで抜く(不透明=写真表示、透明=黒)。アルファのない白黒画像はルミナンス(白=表示)として解釈。
  - マスクのサイズ(表示画面高さに対する %)、中心 X/Y オフセットは両モード共通で調整可能。
- 操作機能: 再生/一時停止(Space)、次へ(→)/前へ(←)、ブラックアウト(B)、ループ再生トグル、写真のドラッグ&ドロップ並べ替え、チェックボックスによる除外(リストには残す)。
- リアルタイム再生に加え、MP4(H.264)書き出し。解像度は 1920×1080 / 3840×2160 / カスタムから選択。fps 既定 30。

## アーキテクチャ(必ず従うこと)

- **electron-vite + React + TypeScript**。ウィンドウは2つ: `control`(操作)と `display`(表示)。
- **再生状態のソース・オブ・トゥルースはメインプロセス**(`src/main/`)。プレイリスト、現在インデックス、再生状態(`idle | playing | paused | blackout`)、タイミング設定、マスク設定を保持し、変更時に両ウィンドウへ IPC でブロードキャストする。
- **毎フレームの IPC は禁止**。表示ウィンドウは「状態 + 基準タイムスタンプ」を受け取り、自前の `requestAnimationFrame` ループで opacity を補間する。
- **タイムライン計算ロジックは `src/shared/` に一元化**し、表示ウィンドウ・操作ウィンドウのプレビュー・書き出しの3箇所すべてで同じ関数を使う。同種のロジックを複数箇所に書かない。
  - 中心となる純粋関数: `(経過時間ms, タイミング設定, 写真枚数, ループ有無) => { 写真インデックス, フェーズ(fadeIn|hold|fadeOut|black), opacity }`
- **マスク処理は canvas 合成(`globalCompositeOperation: 'destination-in'`)に統一**。円形モードも「円を描いたマスク canvas」を生成して同じ描画パスを通す。CSS clip-path は使わない(書き出しと見た目を一致させるため)。
- **写真の配置は stage(出力画面全体)に対する contain・中央配置**(写真全体が見える。足りない部分は黒)。マスクはその上の切り抜きとして働く。マスクモードは `none | circle | image` の3種。
- 表示ウィンドウは `screen` API で外部ディスプレイを検出して自動でフルスクリーン配置。未接続時は通常ウィンドウで表示(開発・プレビュー用)。接続/切断イベントに追従する。
- 書き出しは2段階: ① 各写真を書き出し解像度で「黒背景+stage への contain/cover 配置(既定 contain・写真ごと上書き)+マスクによる切り抜き」済み PNG に canvas 合成 → ② `ffmpeg-static` の ffmpeg で各 PNG を `-loop 1` 静止クリップ化し `fade` フィルタ(in/out)適用、concat して H.264 MP4 出力。フレーム毎のパイプはしない。
- 永続化(プレイリスト・設定)は `electron-store`。
- レンダラーから Node API を直接使わない。`contextIsolation: true` を維持し、`preload` で明示的に API を公開する。

## コーディング規約

- TypeScript strict。`any` を安易に使わない。
- 型定義(IPC のチャンネル名・ペイロード型・共有状態型)は `src/shared/types.ts` に集約し、main / preload / renderer で共有する。
- コメントは「コードから読み取れない制約・理由」のみ書く。自明なコメントは書かない。
- 外部依存は最小限に。UI ライブラリ・状態管理ライブラリは追加しない(React 標準機能で足りる規模)。ドラッグ&ドロップも HTML5 標準 API で実装する。
- 日本語 UI。ラベル・ボタン文言は日本語。

## 検証

- `npm run dev` で起動確認。`npx tsc --noEmit`(または `npm run typecheck`)を変更後に必ず通すこと。
